{
  "name": "prompt-manager",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @prompt-mgr/web-dev dev",
    "build": "pnpm --filter @prompt-mgr/web-dev build",
    "preview": "pnpm --filter @prompt-mgr/web-dev preview",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
